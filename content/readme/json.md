{{JSON_COUNT}} of the {{ROW_COUNT}} prompts here are written that way, and JSON is the longer format when it is used: a median of {{JSON_MEDIAN_CHARS}} characters against {{PROSE_MEDIAN_CHARS}} for the {{PROSE_COUNT}} written as prose. The bias grows with size — {{LONGEST_QUARTILE_JSON_COUNT}} of the {{LONGEST_QUARTILE_COUNT}} longest prompts in the library are JSON objects. Paste the whole object, braces and all; the model reads it as text, and a fragment of one reads as a fragment.
